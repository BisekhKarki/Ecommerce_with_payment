import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["res.cloudinary.com"], // Replace with your actual domain(s) if needed
  },
};

export default nextConfig;
